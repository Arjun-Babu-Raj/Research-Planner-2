
// src/lib/firebase.ts
import { type FirebaseOptions } from 'firebase/app';
import { getAuth } from 'firebase/auth';

const app = null; // Firebase is not used
const auth = null; // Firebase is not used

export { app, auth };
